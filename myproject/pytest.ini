[pytest]
DJANGO_SETTINGS_MODULE = myproject.settings
python_files = tests.py test_*.py *_tests.py

addopts = -v --strict-markers

markers =
    concurrency: check race conditions and lockups (require stable timings)